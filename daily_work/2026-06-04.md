# 2026-06-04 今日工作计划

## 总目标

围绕和平精英全流程自动化的稳定性和效率做专项修复。今天采用“先专项验证，再全流程验证”的节奏：先把断流、无操控超时等运行产物归档问题闭环，再逐项优化搜房、出房、找车上车和车辆避障策略。

## 今日计划表

| 序号 | 优先级 | 模块 | 问题/目标 | 计划动作 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | P0 | 运行归档 | gRPC 流断连后，testcases 设备日志需要进入对应用例目录 | 增加断流提醒日志；将 `DeviceLogger` 生成日志复制到 `aw/autogame/temp/game_cases_xxx/第N次_xxx/logs/` | 模拟或触发断流，检查 `stream_disconnect_device_log_*.txt` 和 `stream_disconnect_notice.txt` | 已完成 |
| 2 | P0 | 运行归档 | 长时间无操控保存的当前帧应进入对应用例目录 | launcher 显式传入本轮 `AUTOGAME_RUN_ARCHIVE_DIR`；无操控截图保存到本轮 `unknow_screenshots/` | 触发无操控超时，检查 `第N次_xxx/unknow_screenshots/unknow_screen_*.jpeg` | 已完成 |
| 3 | P0 | 搜房性能 | 搜房操作后帧回传延迟 1-2s，影响效率 | 增加耗时分段日志，拆分拉流、帧刷新、识别、模型推理、业务等待耗时 | 搜房专项跑一段，输出每阶段耗时统计 | 待开始 |
| 4 | P1 | 出房 | 当前出房速度偏慢，影响整体进度 | 收敛出房搜索、确认、等待和重复刷新逻辑 | 单独进屋后触发出房，统计出房耗时和成功率 | 待开始 |
| 5 | P1 | 找房进门 | 去进门点时距离判断和推进时间不够准确，避障和角度调整不能一步到位 | 优化距离到推进时长映射；调整进门点精细推进和避障后的角度校正 | 专项验证不同距离下到点误差、进门成功率、避障恢复时间 | 待开始 |
| 6 | P1 | 找车上车 | 距离车辆很近时，前推时间太短上不车，太长会过头 | 根据距离、朝向、车辆检测框动态计算前推时长 | 专项验证不同初始距离下能否稳定触发上车 | 待开始 |
| 7 | P2 | 车辆避障 | 当前前景障碍物判断基于矩形区域，不符合驾驶视野 | 改为屏幕归一化坐标定义的前方长梯形 ROI | 对比矩形/梯形 ROI 下的障碍识别结果和误判率 | 待开始 |
| 8 | P2 | 车辆避障 | 现有 `small_left`、`large_left` 等粗调不够精确 | 基于感知和前置状态动态选择前进左转、前进右转、后退左转、后退右转，并计算目标角度 | 驾驶专项验证避障动作是否更短、更稳定 | 待开始 |
| 9 | P2 | 车辆控制 | 需要建立车辆角度控制和按键时间映射表 | 扩展/复用 `TurnCalibration`，缺表时动态生成，执行后用实际角度反馈更新 | 验证计划转 15/20 度时，实际角度误差是否收敛 | 待开始 |
| 10 | P1 | 搜房绕圈 | 搜房一直顺时针，撞墙/门第 6 次后应切逆时针 | 增加顺/逆时针状态切换；第 6 次撞墙/门后改为左转视野并右上滑 | 室内专项验证一圈后是否切换方向，避免持续顺时针卡墙 | 待开始 |
| 11 | P0 | 全流程验证 | 专项修复完成后确认全流程收益 | 跑完整 launcher 流程，归档日志、帧、断流/超时截图和耗时结果 | 检查完整归档目录、阶段耗时、失败点和成功率 | 待开始 |

## 已完成记录

- 已完成断流日志归档：提交 `0f35246 Archive run diagnostics in launcher`。
- 已完成无操控超时当前帧归档：当前帧会保存到本轮用例目录下的 `unknow_screenshots/`。
- 已完成基础静态验证：相关 Python 文件通过 `python3 -m py_compile`。

## 验收顺序

1. 先做运行产物专项验证：断流日志归档、无操控截图归档。
2. 再做搜房链路专项验证：帧延迟、出房、找房进门、搜房绕圈。
3. 再做车辆专项验证：找车上车、梯形 ROI、动态避障、角度时间映射表。
4. 最后跑全流程，确认所有产物都进入同一轮 `game_cases_xxx/第N次_xxx/` 目录。
